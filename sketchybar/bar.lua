local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
  height = 26,
  color = colors.bg1,
  padding_right = 2,
  padding_left = 2,
  padding_bottom = 0,
  position = "top",
})
