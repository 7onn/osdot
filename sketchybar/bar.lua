local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
  height = 30,
  color = colors.bg1,
  padding_right = 5,
  padding_left = 5,
  padding_bottom = 5,
  position = "top",
})
