local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
  height = 30,
  color = colors.bg1,
  corner_radius = 37,
  padding_right = 15,
  padding_left = 15,
  padding_bottom = 15,
  position = "top",
})
